export function ParticleBackground() {
  // Canvas element with ID – the main.js will attach the particle logic to it
  return `<canvas id="particleCanvas" class="fixed inset-0 -z-10 pointer-events-none"></canvas>`;
}